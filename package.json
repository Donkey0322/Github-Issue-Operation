{
  "name": "frontend-intern-homework",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd frontend && yarn && cd ../backend && yarn",
    "frontend": "cd frontend && yarn start",
    "backend": "cd backend && yarn server",
    "test": "npx cypress run --spec cypress/e2e/**/*.cy.js",
    "push": "node git.js push",
    "pull": "node git.js pull && git merge main",
    "clone": "node git.js clone"
  },
  "author": "李昀宸",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.1.0",
    "dotenv-defaults": "^5.0.2",
    "mongoose": "^6.8.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "simple-git": "^3.15.1"
  }
}
